{
  "id": "obsidian-manager",
  "name": "Obsidian Vault Manager",
  "description": "Obsidian vault management with note search, read/write, tagging, wikilink navigation, and git sync.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "vaultPath": {
        "type": "string",
        "description": "Absolute path to the Obsidian vault directory"
      },
      "gitRemote": {
        "type": "string",
        "description": "Git remote URL for vault sync (optional)"
      },
      "gitBranch": {
        "type": "string",
        "description": "Git branch name",
        "default": "main"
      },
      "autoSync": {
        "type": "boolean",
        "description": "Auto-sync vault on changes",
        "default": false
      },
      "excludePatterns": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Glob patterns to exclude from search/listing",
        "default": [".obsidian"]
      }
    }
  }
}
